function removeDuplicates(numbers) {
  return numbers.reduce(
    (accumulator, currentValue) => {
      // TODO: reduce logic
    },
    [] /* TODO: add initial value */
  );
}

// const numbers = [1, 4, 3, 4, 5, 4, 6, 6, 2];
console.log(removeDuplicates([1, 4, 3, 4, 5, 4, 6, 6, 2]));
